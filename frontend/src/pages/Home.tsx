// 首页：统一搜索框 + 筛选 + 最近验证 + 热门知识 + 待认领缺口。
// UI 对照 prototype/kms-prototype.html 的 renderHome()。实现于 M2（依赖 /search /gaps）。
export default function Home() {
  return <h1>首页 · 搜索（M2 实现，布局见 prototype）</h1>;
}
