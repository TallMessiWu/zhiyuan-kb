// 复核队列：触发原因 + AI 变化摘要 + diff 片段 + 折叠 AI 草稿 + 四选一。
// UI 对照 prototype 的 renderReview()。实现于 M4，GET /review + POST /review/{id}/resolve。
export default function Review() {
  return <h1>复核队列（M4 实现）</h1>;
}
