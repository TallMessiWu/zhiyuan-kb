// AI 问答页：引用块（状态/适用版本/更新时间）+ 冲突并列 + not_found 固定话术 + 记缺口。
// UI 对照 prototype 的 renderAsk()/renderAnswer()。实现于 M5，数据来自 POST /api/v1/ask。
export default function Ask() {
  return <h1>AI 问答（M5 实现）</h1>;
}
